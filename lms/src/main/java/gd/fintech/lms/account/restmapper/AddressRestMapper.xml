<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gd.fintech.lms.account.restmapper.AddressRestMapper">
	<!-- 테스트.우편번호를 조건으로 주소 정보 목록을 조회하는 쿼리문 -->
	<select id="selectAddressByRestApi" parameterType="String" resultType="String">
		SELECT 
			CONCAT(_UTF8MB4'(우편번호)',zip_code,
			_utf8mb4' ',province,
			_utf8mb4' ',city,
			_UTF8MB4' ',town,
			_UTF8MB4' ',street) AS 'address'
		FROM address
		WHERE zip_code = #{zipCode}
	</select>
</mapper>